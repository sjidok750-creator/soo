# CLAUDE.md

이 파일은 AI 어시스턴트(Claude 등)가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 저장소 개요

> **현재 상태:** 이 저장소는 초기화된 상태로, 아직 소스 파일이 추가되지 않았습니다.
> 프로젝트가 구성되면 아래 섹션을 실제 내용으로 업데이트하세요.

## 프로젝트 구조

```
soo/
├── CLAUDE.md        # AI 어시스턴트용 가이드 (이 파일)
└── (추후 파일 추가 예정)
```

## 기술 스택

- 현재 미정 — 프로젝트 초기화 후 업데이트 필요

## 개발 환경 설정

```bash
# 저장소 클론
git clone <repository-url>
cd soo

# 의존성 설치 (패키지 매니저 확정 후 업데이트)
# npm install   또는
# pip install -r requirements.txt   또는
# ...
```

## 개발 워크플로

### 브랜치 전략

- `main` / `master` — 프로덕션 브랜치 (직접 푸시 금지)
- `develop` — 통합 개발 브랜치
- `feature/<기능명>` — 기능 개발 브랜치
- `fix/<버그명>` — 버그 수정 브랜치
- `claude/<작업명>` — AI 어시스턴트 작업 브랜치

### 커밋 메시지 규칙

[Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다:

```
<type>(<scope>): <subject>

[optional body]
```

**타입:**
- `feat` — 새로운 기능
- `fix` — 버그 수정
- `docs` — 문서 변경
- `style` — 코드 포맷 변경 (기능 변경 없음)
- `refactor` — 리팩토링
- `test` — 테스트 추가/수정
- `chore` — 빌드 도구, 설정 변경

### PR(Pull Request) 가이드

- PR은 `main` 또는 `develop` 브랜치를 대상으로 생성
- 리뷰어 최소 1명 지정
- 모든 테스트 통과 필수

## 빌드 및 테스트

```bash
# 빌드 (확정 후 업데이트)
# npm run build

# 테스트 실행 (확정 후 업데이트)
# npm test

# 린트 실행 (확정 후 업데이트)
# npm run lint
```

## 코딩 규칙

### 일반 원칙

- 간결하고 읽기 쉬운 코드를 우선시합니다.
- 함수/메서드는 단일 책임 원칙을 따릅니다.
- 명확한 변수명과 함수명을 사용합니다 (축약어 지양).
- 자명한 코드에는 주석을 달지 않습니다. 복잡한 비즈니스 로직에만 설명을 추가합니다.

### 보안

- 비밀키, API 키, 패스워드는 절대 코드에 하드코딩하지 않습니다.
- 환경 변수(`.env`)를 사용하고, `.env` 파일은 `.gitignore`에 포함합니다.
- 사용자 입력은 항상 검증합니다.

## 환경 변수

| 변수명 | 설명 | 필수 여부 |
|--------|------|-----------|
| (추후 추가) | | |

`.env.example` 파일을 참고하여 `.env` 파일을 구성하세요.

## AI 어시스턴트를 위한 지침

### 코드 작성 시

- 기존 코드 스타일과 패턴을 따릅니다.
- 요청된 변경 사항 외에 불필요한 리팩토링을 하지 않습니다.
- 보안 취약점(XSS, SQL 인젝션 등)을 도입하지 않습니다.
- 단순성을 유지합니다 — 현재 필요하지 않은 기능은 추가하지 않습니다.

### Git 작업 시

- `main`/`master`에 직접 푸시하지 않습니다.
- 작업 브랜치는 `claude/<작업명>` 형식으로 생성합니다.
- 커밋 메시지는 위의 규칙을 따릅니다.
- 변경 사항은 커밋하고 지정된 브랜치에 푸시합니다.

### 파일 수정 시

- 파일을 수정하기 전에 반드시 먼저 읽습니다.
- 필요하지 않은 새 파일은 생성하지 않습니다.
- 기존 파일을 수정하는 것을 선호합니다.

---

*이 파일은 프로젝트가 발전함에 따라 업데이트해야 합니다.*
